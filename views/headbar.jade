div(class='navbar-header' id='header_app')
    a(class='navbar-brand', href='/bookp')
        b Simple Library App
nav(class='navbar-collapse collapse')
    ul(class='nav navbar-nav navbar-righ pull-right')
        li(class='dropdown')
            a(href='#', class="dropdown-toggle", data-toggle="dropdown")= user.profile.firstName + ' ' + user.profile.lastName
                b(class='caret')
            ul(class='dropdown-menu')
                if user.rol == 'admin'
                    li
                        a(id='add-user-modal', href='#') Add user
                li
                    a(href='/logout') Logout
    div(class='modal fade')
        div(class='modal-dialog')
            div(class='modal-content' id='addUserModal')
                
script.
    require(['views/headerView']);
